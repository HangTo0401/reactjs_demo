import React, { Component } from 'react'

class ProductItem extends Component {
    render() {
        return (
            <div>Đây là trang chi tiết sản phẩm</div>
        );
    };
}

export default ProductItem;