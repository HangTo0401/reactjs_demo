// Viết CSS in JS
const styles = (theme) => ({
  card: {
    marginTop: 10,
  },
  cardActions: {
    display: "flex",
    justifyContent: "flex-end",
    alignItems: "center",
  },
  deleteIcon: {
    color: '#fafafa',
    backgroundColor: '#f50057'
  }
});

export default styles;
