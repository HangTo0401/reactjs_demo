// Viết CSS in JS
const styles = (theme) => ({
  card: {
    marginTop: 10,
  },
  cardActions: {
    display: "flex",
    justifyContent: "flex-end",
    alignItems: "center",
  },
});

export default styles;
